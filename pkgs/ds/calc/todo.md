1. adding tests cases
2. determine get updates (git submodule, ideally it knows what commit it refactor on then does git diff to see what changed). create skill for this
3. move spec at route (see how AI could know how to pull in correct files eg calc folder if doing calc)

# prompt

i want to make python library code within https://github.com/rubendal/SSBU-Calculator. which should do following

TODO: specify goal is to create skills and make what doing reproduceable and understand how coding so want to know:
1. if any skill files or agent/instruction files should be made
2. how specify other files in spec to look at (eg libray.py)
3. how learns to do this learning activity just mentioning (ie how self improve)
4. how can learn my coding practices (eg __all__ import * preference). difference between scripting vs library coding (and what global vs specific)
5. if any existing repo skills/agent files (and how keep aligned)
6. refactor from existing repo skill (ie when different language and how git diff, etc, likely third party folder)
7. some docs on how use agent prompt (eg fact walk through example and make work)
8. creating tests and running linter when make changes (see how can make fast, ie should we always run tests or not)