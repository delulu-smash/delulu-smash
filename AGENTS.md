<!-- AUTO-GENERATED by GitHub Copilot -->

# Repo Agent Instructions

This is the root config for non-Copilot agent harnesses (eg Codex, Pi). It mirrors the setup already in place for
GitHub Copilot (`.github/copilot-instructions.md`, `.github/instructions/`, `.github/agents/`), but consolidates
everything else under `.agents/` instead of scattering per-folder config files.

Full instructions: @.agents/agents.md