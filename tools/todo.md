create site with reflex. ideas for toolings:
1. interactive with all smash data in one spot (query the "database")
2. recreate the smash tube (but better and more performant)

- see if able to embed these tools into main site via iframes?
- should we do multipage or single page? 