# AUTO-GENERATED by GitHub Copilot
# Omarchy 4 user-configuration record.
# Apply Hyprland bindings through $HOME/.config/hypr/bindings.lua.
# https://omarchy.org/manual/dotfiles/
# TODO: think of doing theme configuration to severance one
# theme = "cattpucinn"
omarchy_version = "4.0.0"
packages = [
    # vscode
    "code"
    ## wf-recorder  ffmpeg wl-clipboard libnotify
]
# options: vscode, nvim, sublime
# NOTE: configure to sublime so when open file in file explorer have fast to open editor
# TODO: decide on editor
# editor = "sublime"
keybindings = [
    { shortcut = "SUPER + SHIFT + Z", name = "Screenshot", cmd = "omarchy screenshot" },
    { shortcut = "SUPER + SHIFT + V", name = "Vscode: delulu-smash", cmd = "code $HOME/repos/delulu-smash" }
]
# TODO: make web app configurations
# NOTE: use dashboardicons.com > select PNG option
# web_apps = [
#     {"name":"Smash", "url": "TBD","icon_png": "TBD"}
# ]
# TOOD: add config for clock header
# nvim ~/.config/omarchy/shell.json
# "format": "dddd M/d HH:mm",
# ensure zed integrated with 1passwrod via https://docs.zen-browser.app/guides/1password