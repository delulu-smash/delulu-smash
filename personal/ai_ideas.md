<!-- AUTO-GENERATED by GitHub Copilot -->

# AI Ideas

Brief backlog for Copilot or repo-organization ideas that seem useful, but are not approved for implementation yet.

## Current Ideas

- Add more scoped instructions under `docs/`, especially separate guidance for `docs/blog/**` and `docs/matchups/**` if those sections need different writing styles.
- Revisit whether `local/` should stay mixed-use or be split into a cleaner fully-ignored output area plus a smaller tracked local-notes area.
- Keep using `.os/README.md` as the source of truth for machine-tooling intent so future AI sessions can recover `.os/` context quickly.
